const Button = function () {
    return (
        <button
            onClick={(e) => {
                // console.log(e.target);
            }}
        >
            PRESS
        </button>
    );
};

export default Button;
